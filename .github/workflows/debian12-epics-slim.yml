---
name: Debian 12 EPICS slim

"on":
  push:
    branches: [master]
    paths:
      - "debian12-epics-slim/**"
      - "gate.bash"
      - ".trigger/**"
      - ".github/workflows/debian12-epics-slim.yml"
      - ".github/workflows/image.yml"
  pull_request:
    branches: [master]
    paths:
      - "debian12-epics-slim/**"
      - "gate.bash"
      - ".github/workflows/debian12-epics-slim.yml"
      - ".github/workflows/image.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  debian12-epics-slim:
    uses: ./.github/workflows/image.yml
    with:
      image_dir: debian12-epics-slim
      image_name: jeonghanlee/debian12-epics-slim
      is_runner: true
